/** @type {import('tailwindcss').Config} */ 
module.exports = {
  content: ["./dist/*.{html,js}"],
  theme: {
    // Some useful comment
    fontFamily: {
      'PS2P': ['"Press Start 2P"'] // Ensure fonts with spaces have " " surrounding it.
      // Some useful comment
    },
  },
  plugins: [
    // Some useful comment
  ],
}
